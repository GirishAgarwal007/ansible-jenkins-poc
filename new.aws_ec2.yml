plugin: aws_ec2
profile: devops
regions:
  - us-east-1

filters:
  tag:Env:
    - Test
    - Prod
  Prodnstance-state-name : running

keyed_groups:
  - prefix: ansible
    key: tags

hostnames:
  - ip-address
